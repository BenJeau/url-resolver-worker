{
  "name": "url-resolver",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "generate-url-shortener-domains": "node scripts/generate-url-shortener-domains.mjs",
    "generate-user-agents": "node scripts/generate-user-agent-pools.mjs",
    "generate-types": "wrangler types",
    "check": "npm run generate-types && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "node scripts/smoke-resolve.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.15.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
